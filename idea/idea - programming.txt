text mechanic 
						text = list of strings
* divide lines into columns 	=by delimeter 	text = list of lists of strings
* like pivottable group by uniqe column values




reprint ressult 


structure extends list<Object>
{

}

columObject(text, column)
{
	
}



surrond(Structure s,list<String> text)
{

while text.hasnext()
{
t = text.next()


if(!currentlyRunning)
{
	while
	{
		beguining += s.beguing.next().toString();
	}

	t = beguining + t;
	currentlyRunning = true;
}

if(currentlyRunning && !s.column.next().equals(t[s.column.getNumber]))
{
	while
	{
		ending += s.ending.next().toString();
	}


	t = t + ending;
	currentlyRunning = false;
}


}
}


reprint(surround(new Structure(prefix, columObject(text, 2), sufix,end))

with surrounding of column

 	=before prefix + column + sufix
				=after val


