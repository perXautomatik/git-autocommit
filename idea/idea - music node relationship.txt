gather tracks

each track is an trackObject with 

with 

//as iterating over all trackObjects you call reletadTo(iterator) and reletadTo will store the the object parametered in reletadTo 
a method called void reletadTo(trackObject)
//return objects stored through reletadTo
a method called set<trackObject> listReletadTo
// simply return 
a metod called boolean isRelated(trackObject)



